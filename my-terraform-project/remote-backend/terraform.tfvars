# terraform.tfvars

# Optionally, override the default values

# S3 Bucket Name (must be unique)
s3_bucket_name = "custom-terraform-state-bucket-123456"

# DynamoDB Table Name (must be unique)
dynamodb_table_name = "custom-terraform-state-locks-123456"
